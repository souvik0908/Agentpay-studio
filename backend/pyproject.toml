[project]
name = "agentpay-studio-backend"
version = "0.1.0"
description = "Backend for AgentPay Studio"
requires-python = ">=3.11"

[tool.uvicorn]
app = "app.main:app"
host = "0.0.0.0"
port = 8000
reload = true
